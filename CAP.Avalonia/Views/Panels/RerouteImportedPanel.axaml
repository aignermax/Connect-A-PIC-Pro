<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:CAP.Avalonia.ViewModels"
             xmlns:loc="using:CAP.Avalonia.Services.Localization"
             x:Class="CAP.Avalonia.Views.Panels.RerouteImportedPanel"
             x:DataType="vm:MainViewModel">

    <!-- Re-route imported (frozen) routes (issue #857): visible while the design holds
         frozen imported routes (or a re-route just ran, so the delta stays readable). -->
    <StackPanel IsVisible="{Binding BottomPanel.RerouteImported.IsPanelVisible}">
        <Separator Margin="0,20,0,10" Background="#3d3d3d"/>
        <TextBlock Text="{loc:Localize Routing.Reroute.Title}" Foreground="Orange" Margin="0,0,0,5" FontWeight="SemiBold"/>

        <TextBlock Foreground="Gray" Margin="0,0,0,6" TextWrapping="Wrap"
                   Text="{Binding BottomPanel.RerouteImported.FrozenCountText}"/>

        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,6">
            <Button Content="{loc:Localize Routing.Reroute.All}"
                    Command="{Binding BottomPanel.RerouteImported.RerouteAllCommand}"/>
            <Button Content="{loc:Localize Routing.Reroute.Selected}"
                    Command="{Binding BottomPanel.RerouteImported.RerouteSelectedCommand}"/>
        </StackPanel>

        <TextBlock Foreground="Gray" Margin="0,0,0,4" TextWrapping="Wrap"
                   IsVisible="{Binding BottomPanel.RerouteImported.HasHandEditedFrozenRoutes}"
                   Text="{Binding BottomPanel.RerouteImported.HandEditedKeptText}"/>

        <TextBlock Foreground="LightGreen" TextWrapping="Wrap"
                   Text="{Binding BottomPanel.RerouteImported.ResultText}"
                   IsVisible="{Binding BottomPanel.RerouteImported.ResultText,
                               Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
    </StackPanel>
</UserControl>
